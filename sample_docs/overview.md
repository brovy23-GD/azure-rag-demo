# Project overview
A retrieval-augmented generation (RAG) system retrieves relevant document passages before generating a response. The local prototype uses lexical retrieval, returns source references, and can optionally generate answers with Azure OpenAI.
